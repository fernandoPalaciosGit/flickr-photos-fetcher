'use strict';

var expect = require('chai').expect;

describe('UI FlickrPhoto', function() {
    it('should transform the data into a single array of objects, each object containing just the data we need', function(next) {
        expect(true).to.be.true;
        next();
    });

    it('should Convert the array of objects into an HTML list', function(next) {
        next();
    });

    it('should Add the HTML to the page.', function(next) {
        next();
    });

});
